#include <stdio.h>
#include <string.h>
#include <stdlib.h>

int main() {
    char hostname[100];
    printf("Please enter the URL:\n");
    scanf("%s", hostname);

    printf("you enter %s\n", hostname);
    return 0;
}
